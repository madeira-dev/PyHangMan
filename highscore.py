class highscore:
    def __init__(self, name, score):
        self.name = name
        self.score = score

    def check_score(self):
        if self.score > self.score:
            return True
        else:
            return False

    def check_name(self):
        if self.name > self.name:
            return True
        else:
            return False
